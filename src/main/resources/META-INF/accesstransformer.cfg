# Too Many Shortcuts (NeoForge port) access transformers.
# Most of KeyMapping's internals are only touched from inside MixinKeyMapping itself
# (Mixin's @Shadow doesn't need AT for that), so this list is short:

# KeyBindingManager.rebuild() iterates KeyMapping.ALL directly.
public net.minecraft.client.KeyMapping ALL

# ToggleAutoJumpKeyMapping / SkinLayerKeyMapping read+write these directly.
public net.minecraft.client.Options autoJump
public net.minecraft.client.Options modelParts
